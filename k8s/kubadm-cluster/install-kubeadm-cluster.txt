# three steps to install kubeadm cluster by aws ec2 

# Step 1: Create three  EC2 instance

# Step 2: Install kubeadm on all three EC2 instances
by using the following  files:
- on master ec2 run ./master.sh
- on worker1 ec2 run ./node1.sh
- on worker2 ec2 run ./node2.sh


# Step 3: Initialize the kubeadm cluster on the master node
follow the pdf file 
```